const Layout = ({ children }) => {
  return (
    <html lang='en'>
      <head>
        <title>Portfolio</title>
      </head>
      <body>{children}</body>
    </html>
  );
};

export default Layout;
